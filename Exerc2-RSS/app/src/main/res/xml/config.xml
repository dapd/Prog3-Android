<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory android:title="GERAL">
        <EditTextPreference
            android:defaultValue="@string/rssfeed"
            android:key="rssfeedlink"
            android:singleLine="true"
            android:title="Link do Feed"
            android:dialogTitle="Troque o link do feed"
            android:dialogMessage="Digite o link"
            android:positiveButtonText="Salvar"
            android:negativeButtonText="Cancelar" />
        <ListPreference
            android:defaultValue="1"
            android:key="periodicidade_pref"
            android:title="Periodicidade de carregamento"
            android:dialogTitle="Altere a periodicidade de carregamento de notícias"
            android:entries="@array/Periodicidade"
            android:entryValues="@array/Periodicidade"
            android:negativeButtonText="Cancelar" />
    </PreferenceCategory>
</PreferenceScreen>